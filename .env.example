# Database - Supabase PostgreSQL (see docs/CONNECT_SUPABASE.md)
DATABASE_URL="postgresql://prisma.[PROJECT-REF]:[PASSWORD]@[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://prisma.[PROJECT-REF]:[PASSWORD]@[REGION].pooler.supabase.com:5432/postgres"

# Supabase Auth (Settings -> API)
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Encryption for storing OpenAI key (32 bytes = 64 hex chars)
# Generate: openssl rand -hex 32
ENCRYPTION_KEY="your-64-char-hex-string"

# Optional: Override OpenAI key via env (otherwise set in Settings UI)
# OPENAI_API_KEY="sk-..."

# App URL
NEXT_PUBLIC_APP_URL="https://nostop.app"
